---
-
  hosts: osx
  gather_facts: no
  vars:
    user:
      name: stanaka
    rbenv:
      versions: 2.2.2
  vars_files:
    - vars.yml
  handlers:
    - include: handler.yml
  tasks:
    - include: tasks.yml
